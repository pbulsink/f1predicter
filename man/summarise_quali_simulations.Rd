% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_race.R
\name{summarise_quali_simulations}
\alias{summarise_quali_simulations}
\title{Summarise Monte Carlo Qualifying Simulation Matrix}
\usage{
summarise_quali_simulations(
  sim_matrix,
  driver_ids,
  season,
  round,
  n_simulations
)
}
\arguments{
\item{sim_matrix}{Integer matrix of size `n_drivers × n_simulations`. Each
cell is the qualifying position of driver `i` in simulation `j`.}

\item{driver_ids}{Character vector of driver IDs (length `n_drivers`).}

\item{season}{Numeric season year.}

\item{round}{Numeric round number.}

\item{n_simulations}{Integer number of simulations.}
}
\value{
A tibble with one row per driver and columns:
  `driver_id`, `season`, `round`, `pole_prob`, `top3_prob`, `top10_prob`,
  `likely_quali_position`, `position_sd`, `.probs`.
}
\description{
Collapses a raw qualifying simulation matrix (drivers × simulations) into a
per-driver summary tibble suitable for downstream formatting.
}
\examples{
\dontrun{
mat <- matrix(sample(1:20, 20 * 1000, replace = TRUE), nrow = 20)
ids <- paste0("driver_", seq_len(20))
summarise_quali_simulations(mat, ids, 2025, 1, 1000)
}
}
