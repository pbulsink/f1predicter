% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_race.R
\name{summarise_simulations}
\alias{summarise_simulations}
\title{Summarise Monte Carlo Simulation Matrix}
\usage{
summarise_simulations(sim_matrix, driver_ids, season, round, n_simulations)
}
\arguments{
\item{sim_matrix}{Integer matrix of size `n_drivers × n_simulations`. Each
cell is the finishing position of driver `i` in simulation `j`, or `NA`
for a DNF.}

\item{driver_ids}{Character vector of driver IDs (length `n_drivers`).}

\item{season}{Numeric season year.}

\item{round}{Numeric round number.}

\item{n_simulations}{Integer number of simulations (used for probability
denominators).}
}
\value{
A tibble with one row per driver and columns:
  `driver_id`, `season`, `round`, `win_prob`, `podium_prob`, `top10_prob`,
  `likely_position`, `expected_points`, `position_sd`, `.probs`.
}
\description{
Collapses a raw simulation matrix (drivers × simulations) into a per-driver
summary tibble suitable for downstream formatting and social posting.
}
\examples{
\dontrun{
mat <- matrix(sample(1:20, 20 * 1000, replace = TRUE), nrow = 20)
ids <- paste0("driver_", seq_len(20))
summarise_simulations(mat, ids, 2025, 1, 1000)
}
}
