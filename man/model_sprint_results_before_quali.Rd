% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models_reg.R
\name{model_sprint_results_before_quali}
\alias{model_sprint_results_before_quali}
\title{Train Sprint Race Models (Before Main Qualifying)}
\usage{
model_sprint_results_before_quali(
  sprint_data = NULL,
  engine = "ranger",
  save_model = TRUE
)
}
\arguments{
\item{sprint_data}{A data frame of sprint-weekend training rows from
`prepare_sprint_training_data()`. When `NULL` (default), data is assembled
automatically.}

\item{engine}{A character string specifying the model engine. One of
`"ranger"` (default), `"glmnet"`, `"nnet"`, `"kernlab"`, `"kknn"`, or
`"ensemble"`.}

\item{save_model}{A logical value. If `TRUE` (default), the trained models
are automatically saved to the path specified in
`options("f1predicter.models")`.}
}
\value{
A named list with elements `"win"`, `"podium"`, `"t10"`,
  `"position"`, and `"position_class"`.
}
\description{
Trains race-result prediction models for sprint weekends using data available
*before* the main qualifying session. The models combine base ensemble
predictions with sprint race features (`sprint_grid`, `sprint_finish_pos`,
`sprint_points`).

The training data is assembled automatically by `prepare_sprint_training_data()`
when `sprint_data` is `NULL`.
}
\examples{
\dontrun{
models <- model_sprint_results_before_quali()
}
}
