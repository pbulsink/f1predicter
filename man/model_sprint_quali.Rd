% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models_reg.R
\name{model_sprint_quali}
\alias{model_sprint_quali}
\title{Train Sprint Qualifying Models (After Sprint Race)}
\usage{
model_sprint_quali(sprint_data = NULL, engine = "ranger", save_model = TRUE)
}
\arguments{
\item{sprint_data}{A data frame of sprint-weekend training rows from
`prepare_sprint_training_data()`. When `NULL` (default), data is assembled
automatically.}

\item{engine}{A character string specifying the model engine. One of
`"ranger"` (default), `"glmnet"`, `"nnet"`, `"kernlab"`, `"kknn"`, or
`"ensemble"`.}

\item{save_model}{A logical value. If `TRUE` (default), the trained models
are automatically saved to the path specified in
`options("f1predicter.models")`.}
}
\value{
A named list with elements `"quali_pole"`, `"quali_pos"`, and
  `"quali_pos_class"`.
}
\description{
Trains qualifying prediction models for sprint weekends using data available
after the sprint race has been completed. The models combine base qualifying
predictions with sprint race features to predict the outcome of the main
qualifying session.
}
\examples{
\dontrun{
models <- model_sprint_quali()
}
}
