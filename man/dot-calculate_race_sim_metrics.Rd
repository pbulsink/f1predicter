% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_race.R
\name{.calculate_race_sim_metrics}
\alias{.calculate_race_sim_metrics}
\title{Compute Per-Driver Race Simulation Metrics}
\usage{
.calculate_race_sim_metrics(
  season,
  circuit_id,
  new_data,
  historical_data = NULL,
  n_recent = 5L,
  params = simulation_params()
)
}
\arguments{
\item{season}{Numeric season year.}

\item{circuit_id}{Character circuit identifier, used to look up
circuit-specific `grid_pos_corr_avg`.}

\item{new_data}{A tibble from `generate_new_data()` containing at least
`driver_id` and `grid_pos_corr_avg`.}

\item{historical_data}{Historical race data from `clean_data()`. If `NULL`,
uses the package's internal loading.}

\item{n_recent}{Integer number of recent races for the recent-form window
(default 5).}

\item{params}{A list of simulation parameters from `simulation_params()`.}
}
\value{
A tibble with columns `driver_id`, `position_sd`, `dnf_rate`.
}
\description{
Derives per-driver `position_sd` and `dnf_rate` for the upcoming race using
the same empirical approach as `calculate_driver_performance()` in
`championship.R`. The mean (`avg_position`) is *not* returned here; it comes
from the ML model via `.predict_position()`.
}
\keyword{internal}
